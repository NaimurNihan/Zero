kind = "web"
previewPath = "/"
title = "SRT Tools"
version = "1.0.0"
id = "artifacts/srt-tools"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21488

[services.development]
run = "pnpm --filter @workspace/srt-tools run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/srt-tools", "run", "build" ]
publicDir = "artifacts/srt-tools/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21488"
BASE_PATH = "/"
